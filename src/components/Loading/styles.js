import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  text: {
    color: '#17bbd9',
    fontSize: 20,
    textAlign: 'center',
    paddingBottom: 40,
  },
});
export default styles;
